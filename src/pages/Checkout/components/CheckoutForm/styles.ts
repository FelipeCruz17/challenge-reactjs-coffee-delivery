import styled from 'styled-components'
import { FormBaseContainer } from '../../styles'

export const CheckoutFormContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  width: 40rem;
`
export const FormSectionContainer = styled(FormBaseContainer)`
  display: flex;
  flex-direction: column;
  gap: 2rem;
`
